# billiard-score-fe-antd

Frontend for Billiard Score app (React + Vite + Ant Design).

## Setup (local)

1. Ensure backend is running at `http://localhost:4000` or update `.env` `VITE_API_BASE`.
2. Install & run:
```bash
npm install
npm run dev
